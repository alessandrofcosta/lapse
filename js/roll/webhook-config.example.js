// webhook-config.example.js — TRACKED IN GIT (no real secrets here)
// Copy this file to webhook-config.js and fill in the real Discord webhook URLs.
// webhook-config.js is gitignored and must never be committed.

window.WEBHOOK_CONFIG = {
  playerUrl: 'PASTE_PLAYER_DISCORD_WEBHOOK_URL_HERE',
  npcUrl: 'PASTE_NPC_DISCORD_WEBHOOK_URL_HERE',
}
